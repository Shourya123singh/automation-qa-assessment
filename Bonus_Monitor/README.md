
# Bonus Task: Automated Application Uptime Monitor
   This workflow runs continuously on a 5-minute cron interval, routing network metadata directly from the target test web application application. It evaluates response status codes down a true failure path to fire immediate alerts to our notification endpoints if anything other than an HTTP 200 code returns.
